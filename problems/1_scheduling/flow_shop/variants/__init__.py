"""
Flow Shop Variants

This package contains implementations of flow shop scheduling variants
beyond the standard permutation flow shop problem (PFSP).

Variants implemented:
    - No-Wait Flow Shop (NWFSP): Jobs cannot wait between consecutive machines.
    - Blocking Flow Shop (BFSP): A job blocks a machine until the next machine
      becomes available (no intermediate buffers).
"""
